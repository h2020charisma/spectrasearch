# The app reads frontend runtime config from public/config.json in local builds.
# This optional value changes the test-only API origin used by Cypress intercepts.
API_BASE_URL="https://spectrasearch.test.invalid/"
